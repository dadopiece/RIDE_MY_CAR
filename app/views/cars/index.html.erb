<div class="py-4 px-3 bg-white shadow" style="position: sticky; top: 0; z-index: 10">
  <%= form_tag cars_path, method: :get, class: "d-flex justify-content-evenly align-items-center filter-form" do %>
    <div class="form-group mx-2">
      <%= label_tag :brand, 'Marque', class: "form-label" %>
      <%= text_field_tag :brand, params[:brand], class: "form-control custom-input", placeholder: "Marque" %>
    </div>

    <div class="form-group mx-2">
      <%= label_tag :model, 'Modèle', class: "form-label" %>
      <%= text_field_tag :model, params[:model], class: "form-control custom-input", placeholder: "Modèle" %>
    </div>

    <div class="form-group mx-2">
      <%= label_tag :address, 'Adresse', class: "form-label" %>
      <%= text_field_tag :address, params[:address], class: "form-control custom-input", placeholder: "Adresse" %>
    </div>

    <div class="form-group mx-2">
      <%= label_tag :max_price, 'Prix Max', class: "form-label" %>
      <%= number_field_tag :max_price, params[:max_price], class: "form-control custom-input", placeholder: "Prix Max" %>
    </div>

    <%= submit_tag 'Filtrer', class: "btn custom-button" %>
  <% end %>
</div>


<div class="container mt-3" data-controller="switch">

    <div class="container mt-3 text-center">
  <div class="btn-group" data-switch-target="tabs" >
    <button data-action="click->switch#handleClick" data-switch-toggableTarget-param="list" class="btn btn-primary btn-toggle active">
      LIST 📋
    </button>
    <button data-action="click->switch#handleClick" data-switch-toggableTarget-param="map" class="btn btn-outline-primary btn-toggle">
      MAP 🌍
    </button>
  </div>
</div>

<div data-switch-target="cards" class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4">
  <% @cars.each do |car| %>
    <div class="col">
      <div class="card h-100 card-custom">
        <% if car.photo.attached? %>
          <%= cl_image_tag car.photo.key, height: 300, width: 300, crop: :fill, class: "card-img-top" %>
        <% else %>
          <div class="card-img-top placeholder-image">Pas d'image</div>
        <% end %>
        <div class="card-body">
          <h5 class="card-title">
            <%= car.brand %> <%= car.model %>
          </h5>
          <p class="card-text">
            Quelques détails supplémentaires sur la voiture ici.
          </p>
          <p class="card-text price-text">
            <strong><%= number_to_currency(car.price, unit: "€", delimiter: " ", format: "%n %u par jour") %></strong>
          </p>
        </div>

          <div class="card-footer">
            <button data-action="click->modal#launch" data-url="<%= car_path(car, layout: false) %>" class="btn btn-primary btn-custom"> Réserver</button>
          </div>

      </div>
    </div>
  <% end %>
</div>

<div class="d-none"
    style="width: 100%; height: 600px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
    data-switch-target="map">
  </div>
</div>
